@model IEnumerable<WebApp.Models.NotaCorretagemIndexViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4>Nota de Corretagem - Listagem</h4>

<p>
    <div class="col-xl-2 col-lg-4">
        @Html.ActionLink("Inserir Nota", "Inserir", "NotaCorretagem", null, new { @class = "btn btn-primary" })
    </div>
</p>

<table class="table">
    <thead>
        <tr>
            <th class="text-center">Data</th>
            <th class="text-right">Contratos Negociados</th>
            <th class="text-right">Total Líquido da Nota (R$)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (NotaCorretagemIndexViewModel item in Model)
        {
            <tr>
                <td class="text-center">
                    @Html.ActionLink(item.Data, "Detalhe", new { id = item.ID })
                </td>
                <td class="text-right">
                    @Html.ActionLink(item.ContratosNegociados, "Detalhe", new { id = item.ID })
                </td>
                <td class="text-right">
                    @Html.ActionLink(item.TotalLiquidoNota, "Detalhe", new { id = item.ID })
                </td>
            </tr>
        }
    </tbody>
</table>
